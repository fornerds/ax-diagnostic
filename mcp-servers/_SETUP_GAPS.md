# 발급위치 미완 — 캡처/제공 필요 (8 / 81)

분류 전수 결과(2026-06-28): **73개 완전, 8개 미완.** 아래 8개는 발급 콘솔이 **로그인 게이트/계정별**이라 정확한 메뉴·버튼·화면을 외부에서 확인 불가. 해당 계정 접근 가능 시 **스크린샷(주소창 URL + 메뉴/버튼 보이게)**을 주면 SETUP.md에 반영한다. 못 주는 건 "미확인" 유지.

| 커넥터 | 캡처/제공할 것 |
|--------|----------------|
| `flarelane` | console.flarelane.com 로그인 → REST API 키 발급/조회 화면의 좌측 메뉴 경로 + 버튼 라벨 |
| `barobill` | dev.barobill.co.kr 대시보드 → CERTKEY 표시 위치 + 메뉴 라벨 + 복사 버튼명 |
| `ppurio` | 뿌리오/비즈뿌리오 콘솔 → "연동(API) 신청" 메뉴 + 계정ID/인증키 발급 화면 |
| `daglo` | developers.daglo.ai/console → 토큰 메뉴 위치 + 발급 버튼 라벨 |
| `tossplace` | developers.tossplace.com → 앱(API 타입) 생성 + Access Key/Secret 발급 화면 |
| `kakao_work` | 카카오워크 Admin → 봇 App Key 발급 메뉴 경로 + "개발자 등록/봇 생성" 버튼 |
| `line_works` | Developer Console → 앱 등록 Client ID/Secret + Service Account 개인키 발급 화면 |
| `okpos` | (화면 아님) 오픈API/외부개발자 자격증명 발급 문의 연락처(전화/이메일) |

> 나머지 73개는 발급 콘솔 URL + 메뉴>버튼 경로가 확정됨(각 `SETUP.md`). 직링크가 워크스페이스/계정별이라 구조적으로 없는 경우는 "직링크 없음 + 메뉴 경로"로 명시됨.
